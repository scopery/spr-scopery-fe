/**
 * Single page gutter. AppShell applies this on normal routes.
 * Immersive routes (shell `p-0`) apply the same class on the page view.
 */
export const PAGE_GUTTER_CLASS = 'px-3 py-3 lg:px-4 lg:py-3'
