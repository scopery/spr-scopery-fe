'use client'

import { AiControlAdminLayout } from '@/modules/ai-agent-admin/presentation/ui/AiControlAdminLayout'

export default function AdminAiControlLayout({ children }: { children: React.ReactNode }) {
  return <AiControlAdminLayout>{children}</AiControlAdminLayout>
}
