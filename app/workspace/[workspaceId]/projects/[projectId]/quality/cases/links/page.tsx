'use client'

import { UseCaseTestCaseLinksView } from '@/modules/quality'

/** Simplified Quality Cases entry — same UC ↔ TC linker. */
export default function QualityCasesLinksPage() {
  return <UseCaseTestCaseLinksView />
}
