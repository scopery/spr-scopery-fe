/**
 * Shared surface class names — styles live in `app/globals.css`.
 * Use these instead of repeating border/bg utilities so canvas + control
 * surfaces can be retuned in one place.
 */
export const uiControl = 'ui-control'
export const uiDropdownPanel = 'ui-dropdown-panel'
export const uiBtnOutline = 'ui-btn-outline'
